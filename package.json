{
  "scripts": {
    "postinstall": "\"cd app && npm install && \"cd server && npm install",
    "dev": "concurrently \"cd server && npm run dev \" \"cd app && npm start\""
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
